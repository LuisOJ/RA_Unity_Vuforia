# ----------------------------
# Unity + Vuforia - .gitignore completo
# Coloca este archivo en la raíz del proyecto
# ----------------------------

# --- Carpetas generadas por Unity (NO subir)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
.UnityCache/

# Unity incremental build / intermediate
Library/Bee/
Library/PackageCache/
Library/ShaderCache/
Library/com.unity.scriptablebuildpipeline/

# Unity temporary/utmp (unidad temporal de compilación)
.utmp/

# Memory captures / recordings (pueden ser enormes)
[Mm]emoryCaptures/
[Rr]ecordings/

# --- System / IDE / build files
.vs/
.vscode/
.idea/
*.sln
*.csproj
*.unityproj
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# --- Mac / Windows
.DS_Store
Thumbs.db
ehthumbs.db

# --- Android / Gradle / Build artifacts
.gradle/
gradle-app.setting
*.apk
*.aab

# --- Packages / caches / tarballs (Vuforia suele dejar tgz en Packages/)
Packages/*.tgz
/Packages/*.tgz
/Packages/**/*.tgz
Library/PackageCache/

# --- Unity generated meta files (NO ignorar .meta de assets reales)
# (No pongas una regla que ignore *.meta globalmente)

# --- Addressables / StreamingAssets / Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# --- Editor specific / generated
Assets/Plugins/**/obj/
Assets/Plugins/**/bin/
ExportedObj/
.consulo/

# --- Logs, crash reports
sysinfo.txt
crashlytics-build.properties
*.log
*.cache
*.tmp

# --- Modelos 3D: IGNORAR (no subir modelos 3D)
*.fbx
*.obj
*.dae
*.3ds
*.blend
*.stl
*.ply
*.gltf
*.glb
*.max
*.lwo
*.x

# --- Binarios nativos / librerías / paquetes grandes
*.aar
*.so
*.dylib
*.dll
*.a
*.lib
*.tgz
*.zip
*.7z
*.rar

# --- Imágenes / texturas y thumbnails (opcionales: si las necesitas, quítalas)
#*.png
#*.jpg

# --- Node/npm (si se usa)
node_modules/

# --- Otros temporales
*.bak
*.orig
*.old

# ----------------------------
# Opcional: mantener carpeta vacía (ejemplo Assets/Models/.gitkeep)
# Si quieres mantener la carpeta Assets/Models vacía en el repo, crea
# Assets/Models/.gitkeep y NO la ignores (añadir excepción si ignoras la carpeta entera)
# Ejemplo de excepción si usas una regla que ignore Assets/Models/:
# !Assets/Models/.gitkeep
# ----------------------------
# ----------------------------
# Unity + Vuforia - .gitignore completo
# Coloca este archivo en la raíz del proyecto
# ----------------------------

# --- Carpetas generadas por Unity (NO subir)
[Ll]ibrary/
[Tt]emp/
[Oo]bj/
[Bb]uild/
[Bb]uilds/
[Ll]ogs/
[Uu]ser[Ss]ettings/
.UnityCache/

# Unity incremental build / intermediate
Library/Bee/
Library/PackageCache/
Library/ShaderCache/
Library/com.unity.scriptablebuildpipeline/

# Unity temporary/utmp (unidad temporal de compilación)
.utmp/

# Memory captures / recordings (pueden ser enormes)
[Mm]emoryCaptures/
[Rr]ecordings/

# --- System / IDE / build files
.vs/
.vscode/
.idea/
*.sln
*.csproj
*.unityproj
*.suo
*.user
*.userprefs
*.pidb
*.booproj
*.svd
*.pdb
*.mdb
*.opendb
*.VC.db

# --- Mac / Windows
.DS_Store
Thumbs.db
ehthumbs.db

# --- Android / Gradle / Build artifacts
.gradle/
gradle-app.setting
*.apk
*.aab

# --- Packages / caches / tarballs (Vuforia suele dejar tgz en Packages/)
Packages/*.tgz
/Packages/*.tgz
/Packages/**/*.tgz
Library/PackageCache/

# --- Unity generated meta files (NO ignorar .meta de assets reales)
# (No pongas una regla que ignore *.meta globalmente)

# --- Addressables / StreamingAssets / Packed Addressables
/[Aa]ssets/[Aa]ddressable[Aa]ssets[Dd]ata/*/*.bin*
/[Aa]ssets/[Ss]treamingAssets/aa.meta
/[Aa]ssets/[Ss]treamingAssets/aa/*

# --- Editor specific / generated
Assets/Plugins/**/obj/
Assets/Plugins/**/bin/
ExportedObj/
.consulo/

# --- Logs, crash reports
sysinfo.txt
crashlytics-build.properties
*.log
*.cache
*.tmp

# --- Modelos 3D: IGNORAR (no subir modelos 3D)
*.fbx
*.obj
*.dae
*.3ds
*.blend
*.stl
*.ply
*.gltf
*.glb
*.max
*.lwo
*.x

# --- Binarios nativos / librerías / paquetes grandes
*.aar
*.so
*.dylib
*.dll
*.a
*.lib
*.tgz
*.zip
*.7z
*.rar

# --- Imágenes / texturas y thumbnails (opcionales: si las necesitas, quítalas)
#*.png
#*.jpg

# --- Node/npm (si se usa)
node_modules/

# --- Otros temporales
*.bak
*.orig
*.old

# ----------------------------
# Opcional: mantener carpeta vacía (ejemplo Assets/Models/.gitkeep)
# Si quieres mantener la carpeta Assets/Models vacía en el repo, crea
# Assets/Models/.gitkeep y NO la ignores (añadir excepción si ignoras la carpeta entera)
# Ejemplo de excepción si usas una regla que ignore Assets/Models/:
# !Assets/Models/.gitkeep
# ----------------------------
